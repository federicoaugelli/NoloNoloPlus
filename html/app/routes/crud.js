const express = require('express');
const router = express.Router();
const registrodipendenti = require('../model/clientiModel.js');
const fs = require('fs');

/*
router.get("/example", function (req, res){
    registroclienti.find({})
    .exec(function (err, examples){
        if (err) {
            res.send("an error has occurred")
        } else res.render(examples: examples);
        });
    });



const dipendentiData = fs.readFileSync('C:/Users/matte/Documents/GitHub/site202127/html/public/data/dipendenti.json');
let dipendenti = JSON.parse(dipendentiData);


router.post("/createDipendentiCollection", function (req, res){

    registrodipendenti.insertMany(dipendenti);
    res.redirect('/backend');
 });

 module.exports = router;
 */